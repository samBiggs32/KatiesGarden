name: CI

on:
  push:
    branches-ignore:
      - main  # production-deploy.yml handles main

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    name: Build & Unit Tests
    steps:
      - uses: actions/checkout@v5

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'

      - name: Build client
        run: dotnet build Web/KatiesGarden.Web/Client/KatiesGarden.Web.Client.csproj --configuration Release

      - name: Build API
        run: dotnet build Api/KatiesGarden.Api.csproj --configuration Release

      - name: Build Aspire AppHost
        run: dotnet build AppHost/KatiesGarden.AppHost.csproj --configuration Release

      - name: CVE dependency gate
        run: |
          set -euo pipefail
          # Restore first: `dotnet list package --vulnerable` needs restored assets, and
          # passing the solution explicitly avoids the "no project found" ambiguity that
          # would otherwise let the grep miss and the step exit 0 (fail-open).
          dotnet restore KatiesGarden_Wasm.sln
          OUTPUT=$(dotnet list KatiesGarden_Wasm.sln package --vulnerable --include-transitive)
          echo "$OUTPUT"
          if echo "$OUTPUT" | grep -q "has the following vulnerable"; then
            echo "ERROR: Vulnerable packages found — fix before merging"
            exit 1
          fi
          echo "PASS: No known CVEs in dependency tree"

      - name: Run unit tests
        run: dotnet test Tests/KatiesGarden.Tests/KatiesGarden.Tests.csproj --configuration Release --filter "Category!=Integration" --logger "trx;LogFileName=unit-results.trx"

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: unit-test-results-${{ github.sha }}
          path: "**/unit-results.trx"

  integration_tests:
    runs-on: ubuntu-latest
    name: Integration Tests (Postgres + Aspire smoke)
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'

      - name: Verify Docker
        run: docker info

      - name: Install Azure Functions Core Tools
        run: npm install -g azure-functions-core-tools@4

      - name: Run integration tests
        run: dotnet test Tests/KatiesGarden.Tests/KatiesGarden.Tests.csproj --configuration Release --filter "Category=Integration" --logger "trx;LogFileName=integration-results.trx"

      - name: Upload integration test results
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: integration-test-results-${{ github.sha }}
          path: "**/integration-results.trx"
